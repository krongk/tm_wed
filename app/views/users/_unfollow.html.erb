<% user = @other_user || other_user %>
<%= form_for(current_user.relationships.find_by(followed_id: user.id),
             html: { method: :delete, class: 'form-inline' },
             remote: true) do |f| %>
  <%= f.submit "取消订阅", class: "btn btn-default btn-xs" %>
<% end %>
