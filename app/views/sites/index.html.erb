<h1>应用列表</h1>

<table class="table table-bordered">
  <tbody>
    <% @sites.each do |site| %>
      <tr>
        <td><%= site.id %> </td>
        <td><%= site.title %> </td>
        <td><%= site.description %> </td>
        <td><%= site.status || '免费' %> </td>
        <td><%= link_to '预览', get_site_url(site), target: '_blank' %></td>
        <td><%= link_to '查看', site %></td>
        <td><%= link_to '修改', edit_site_path(site) %></td>
        <td><%= link_to '删除', site, method: :delete, data: { confirm: '确定要删除?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to '创建新应用', new_site_path, class: 'btn btn-meflat' %>
